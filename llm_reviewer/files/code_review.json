[
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 1,
        "problem": "Direct style injection in CSS file.",
        "suggestion": "Use Styled Components or Emotion to encapsulate styles for isolated components.",
        "before": "import './PaginaPrincipal.css';",
        "after": "import { styled } from 'react-styled'; import './PaginaPrincipal.css'; // Use styled() to inject style"
    },
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 3,
        "problem": "Multiple states for a single component.",
        "suggestion": "Use a single state variable for the button's visibility and update it accordingly.",
        "before": "const [visible, setVisible] = useState(true);",
        "after": "const [isVisible, setIsVisible] = useState(true); // Use a single state variable for the button's visibility"
    },
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 7,
        "problem": "Inline logic in render function.",
        "suggestion": "Move logic to a separate method or use an effect to run on each render.",
        "before": "return (<div>...</div>);",
        "after": "const handleClick = () => { setIsVisible(!isVisible); }; return (<div><h1>Home</h1><button onClick={handleClick}>Toggle Visibilidade</button><div className='extra-div'>{isVisible && <p>Conteúdo visível!</p>}</div></div>);"
    },
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 14,
        "problem": "Direct style injection in CSS file.",
        "suggestion": "Use Styled Components or Emotion to encapsulate styles for isolated components.",
        "before": "import './PaginaPrincipal.css';",
        "after": "import { styled } from 'react-styled'; import './PaginaPrincipal.css'; // Use styled() to inject style"
    },
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 17,
        "problem": "Multiple states for a single component.",
        "suggestion": "Use a single state variable for the button's visibility and update it accordingly.",
        "before": "const [visible, setVisible] = useState(true);",
        "after": "const [isVisible, setIsVisible] = useState(true); // Use a single state variable for the button's visibility"
    },
    {
        "file": "src/pages/PaginaPrincipal.js",
        "line": 19,
        "problem": "Inline logic in render function.",
        "suggestion": "Move logic to a separate method or use an effect to run on each render.",
        "before": "return (<div>...</div>);",
        "after": "const handleClick = () => { setIsVisible(!isVisible); }; return (<div><h1>Home</h1><button onClick={handleClick}>Toggle Visibilidade</button><div className='extra-div'>{isVisible && <p>Conteúdo visível!</p>}</div></div>);"
    }
]